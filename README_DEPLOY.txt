Alfa Portal VIP — Netlify bundle
===============================

_redirects (SPA): /* /index.html  200 — from artifacts/gambling-affiliate/public/

Optional: netlify-functions/api.mjs when built without NETLIFY_API_PROXY_TARGET.
Add /api routing in Netlify UI or netlify.toml if you need same-origin API with Functions.
